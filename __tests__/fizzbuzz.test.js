// intentionally incomplete so I can live demo

const fizzBuzz = require("../src/fizzbuzz");

describe("what function are we testing?", () => {
    test("what should our test be testing?", () => {
        expect("what function?"("what argument?")).toBe("what outcome?");
    });
});